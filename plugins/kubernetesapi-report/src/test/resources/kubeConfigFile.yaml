apiVersion: v1
kind: Config

clusters:
  - cluster:
      server: https://kubernetes.cluster-c.local
    name: cluster-c

users:
  - name: unittest

contexts:
  - context:
      cluster: cluster-c
      user: unittest
    name: cluster-c
